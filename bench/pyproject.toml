[project]
name = "shifthtml-bench"
version = "0.0.0"
description = "Benchmarks for shifthtml vs jinja2, minijinja, and tdom."
requires-python = ">=3.14"
dependencies = [
    "shifthtml",
    "jinja2",
    "minijinja",
    "tdom",
]

[tool.uv.sources]
shifthtml = { path = ".." }
